.vimや.vimrcをgoogleドライブにバックアップしてある？場合はダウンロードしておく
バックアップしてない場合
NeoBundleのダウンロードと設置
# 配置先のディレクトリを作成
$ mkdir -p ~/.vim/bundle
# NeoBundleをリポジトリから取得
$ git clone git://github.com/Shougo/neobundle.vim ~/.vim/bundle/neobundle.vim
をやる
あとはNeoBundleを設定する
